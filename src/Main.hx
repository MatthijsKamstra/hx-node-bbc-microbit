package;

import examples.*;

/**
 * @author Matthijs Kamstra aka [mck]
 */
class Main {

	public function new () {

		// new AccelerometerListener();
		new ButtonListener();
		// new Compass();
		// new Discoverer();
		// new EventListener(); // doesn't work: the code on github is not update to NPM
		// new LedPattern();
		// new LedPatternTest();
		// new LedPatternHx();
		// new LedText();
		// new MagnetometerBearingListener();
		// new MagnetometerListener();
		// new PinBlink();
		// new PinListener();
		// new TemperatureListener();

	}



	static public function main () {
		new Main ();
	}
}
